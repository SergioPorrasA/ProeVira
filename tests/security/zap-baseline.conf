# Configuración OWASP ZAP Baseline Scan
mode=baseline
config.recurse=true
config.failOnWarn=true
config.maxDuration=15
config.contextFile=
config.ajaxSpider=false
policy.file=
# Excluir endpoints de logout si es necesario
# config.exclude=.*\/logout$
